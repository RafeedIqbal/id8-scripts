---
name: id8
description: Run the full /id8 product-to-deploy workflow in strict sequence. Use when the user asks for /id8 execution, a PRD-to-code-to-deploy flow, or staged delivery with MCP integrations (Context7, Stitch, GitHub, Vercel, Supabase).
---

# /id8 Workflow

Invocation: `{{INVOCATION}}`

Follow the sequence exactly. Never combine multiple steps into one response.

## Strict Step Sequence
{{WORKFLOW_STEPS}}

## Dry-Run Mode
{{DRY_RUN_CHECKS}}

## Confirmation Gates
{{CONFIRMATION_GATES}}

## Execution Rules

1. Do not hallucinate tool output.
2. Do not run Steps 7, 8, or 9 without explicit approval.
3. If a required MCP server is not authenticated, stop and report the required login/action.
4. If `--dry-run` is set, perform connectivity checks only.
5. Before each MCP call, validate parameter names and types against the current tool schema.
