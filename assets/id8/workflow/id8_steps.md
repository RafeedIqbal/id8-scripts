1. Generate a PRD from the initial prompt.
   Pause after output. Ask for confirmation before continuing.
2. Call Context7 and produce a technical plan compatible with Vercel and Supabase.
   Pause after output. Ask for confirmation before continuing.
3. Ask the user if they want the design to be generated by AI or not.
   If yes, ask what device type to design for, then call Stitch create a new project, then generate the UI screens required and collect user review/adjustments.
   Pause after user feedback loop finishes.
   Use list_screens tool to list the screens generated, provide links to the screens to the user.
4. Prompt user to create project directory or generate in this folder, and ask for repository model (monorepo vs split repos), then create directories for the project.
   Implement backend code using the approved tech plan and Context7 references.
   Pause after implementation summary.
5. Implement frontend code using approved Stitch output (if generated), tech plan and Context7 references.
   Pause after implementation summary. 
6. Run local test suites and report pass/fail with actionable fixes.
   Pause after presenting test results. 
7. Create a .gitignore file to exclude unnecessary files.
   Do a security check to see if any keys are being pushed, stop and notify user if found.
   Then call GitHub MCP to create repo(s) for project if needed and push code.
   Mandatory confirmation required immediately before any repo creation/push action.
8. Call Vercel MCP to deploy the frontend.
   Mandatory confirmation required immediately before deployment.
9. Call Supabase MCP to provision/deploy backend resources.
   Mandatory confirmation required immediately before provisioning/deployment.
10. Perform post-deploy verification checks and publish a completion report with links, status, and outstanding actions.
    Pause after final report for explicit closeout confirmation.
