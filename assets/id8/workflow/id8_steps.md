1. Generate a PRD from the initial prompt. Ask to avoid using things that require outside API keys, use keyless APIs if needed.
   Pause after output. Ask for confirmation before continuing.
2. Call Context7 and produce a technical plan compatible with Vercel and Supabase. Ask to avoid using things that require outside API keys, use keyless APIs if needed.
   Pause after output. Ask for confirmation before continuing.
3. Ask the user if they want the design to be generated by AI or not.
   If yes, ask what device type to design for, then call Stitch MCP tools (e.g., `create_project`) to create a new project, then generate the UI screens required (e.g., `generate_screen_from_text`) and collect user review/adjustments.
   Pause after user feedback loop finishes.
   Use the Stitch MCP `list_screens` tool to list the screens generated, provide links to the screens to the user.
4. Create 2 folders: one for the frontend (Vercel project) and one for files needed for the backend (Supabase deployment). 
   Implement backend code in the backend folder using the approved tech plan and Context7 references.
   Pause after implementation summary.
5. Implement frontend code in the frontend folder using approved Stitch output (if generated), the tech plan, and Context7 references.
   Pause after implementation summary. 
6. Run both frontend and backend locally once instead of writing standard tests. Run tests for the backend if relevant.
   Report pass/fail with actionable fixes.
   Pause after presenting local run results. 
7. Create a `.gitignore` file to exclude unnecessary files.
   Do a security check to see if any keys are being pushed, stop and notify user if found.
   Check if the current directory is already in a Git repository. If not, ask if the user wants to have the current working directory as the repo (monorepo), or create 2 new ones (frontend repo and backend repo).
   Then call GitHub MCP server (e.g., `create_repository`, `push_files`) to create repo(s) for the project if needed and push code.
   Mandatory confirmation required immediately before any repo creation/push action.
8. Deploy the backend first. Call Supabase MCP tools (e.g., `create_project`, `apply_migration`, `get_project_url`, `get_publishable_keys`) to provision/deploy backend resources.
   Note the Supabase URL and public key, to use for Vercel deployment.
   Mandatory confirmation required immediately before provisioning/deployment.
9. Deploy the frontend after the backend. Call Vercel MCP (e.g., `deploy_to_vercel`) to deploy the frontend. Use the Supabase URL and public key obtained from the previous step as environment variables.
   Mandatory confirmation required immediately before deployment.
10. Perform post-deploy verification checks and publish a completion report with links, status, and outstanding actions.
    Pause after final report for explicit closeout confirmation.
