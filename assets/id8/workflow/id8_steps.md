1. Generate a PRD from the initial prompt. Ask the user for a project title, or suggest one if not provided.
   Pause after output. Ask for confirmation before continuing.
2. Call Context7 and produce a technical plan compatible with Vercel and Supabase. Avoid using services that require outside API keys, use keyless APIs if needed.
   Pause after output. Ask for confirmation before continuing.
3. Ask the user if they want the design to be generated by AI or not.
   If yes, ask what device type to design for, then call Stitch MCP tools (e.g., `create_project`) to create a new project, then generate the UI screens required (e.g., `generate_screen_from_text`) and collect user review/adjustments.
   Pause after user feedback loop finishes.
   Use the Stitch MCP `list_screens` tool to list the screens generated, provide links to the screens to the user.
4. Create a folder with project name, with two sub folders one for the Vercel project and one for files needed for the Supabase deployment. 
   If there is supabase functionality required, implement supabase code in the supabase folder using the approved tech plan and Context7 references.
   Pause after implementation summary.
5. Implement frontend code in the frontend folder using approved Stitch output with `get_screen` tool (if generated), the tech plan, and Context7 references.
   Ensure the frontend matches the Stitch design as closely as possible and fully implements all features from the PRD.
   The AI should not ask before proceeding to the next step unless there is something for the user to review.
   Pause after implementation summary. 
6. Run frontend locally and fix any issues, retry until all issues fixed. Run tests for the backend if relevant.
   Pause after presenting local run results. 
7. Create a `.gitignore` file to exclude unnecessary files.
   Do a security check to see if any keys are being pushed, stop and notify user if found.
   Check if the current directory is already in a Git repository. If not, ask if the user wants to create a repo for the project folder.
   Then call GitHub MCP server (e.g., `create_repository`, `push_files`) to create repo(s) for the project if needed and push code.
   Mandatory confirmation required immediately before any repo creation/push action.
8. If supabase code was implemented, deploy the backend first. Call Supabase MCP tools (e.g., `create_project`, `apply_migration`, `get_project_url`, `get_publishable_keys`) to provision/deploy backend resources. 
   Make sure RLS is enabled in the Supabase project.
   Note the Supabase URL and public key, to use for Vercel deployment.
   Mandatory confirmation required immediately before provisioning/deployment.
9. Deploy the frontend after the backend. Before deploying, securely add the Supabase URL and public key as environment variables to the Vercel project via the Vercel CLI.
   Ensure keys are not exposed in standard outputs and RLS is enabled in the Supabase project.
   Use the following sequence within the frontend directory:
   ```bash
   export NEXT_PUBLIC_SUPABASE_URL="<Supabase_URL>" && \
   export NEXT_PUBLIC_SUPABASE_ANON_KEY="<Supabase_Anon_Key>" && \
   npx vercel env add NEXT_PUBLIC_SUPABASE_URL production < <(echo "$NEXT_PUBLIC_SUPABASE_URL") && \
   npx vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production < <(echo "$NEXT_PUBLIC_SUPABASE_ANON_KEY")
   ```
   Then call Vercel MCP (e.g., `deploy_to_vercel`) or run `npx vercel deploy --prod` to deploy the frontend.
   Mandatory confirmation required immediately before deployment.
10. Perform post-deploy verification checks and publish a completion report with links, status, and outstanding actions.
    Pause after final report for explicit closeout confirmation.
